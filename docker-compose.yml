version: '3.8'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sarpras
    restart: always
    env_file:
      - .env
    ports:
      - 8080:3000
    volumes:
      - /app/node_modules
      